/**
 * Onboarding Automation
 * Triggers welcome flows, tours, and starter automations.
 */

export function triggerOnboarding(tenantId: string, userId: string) {
  // Would queue welcome email, create sample project, enable basic automations, etc.
  console.log(`[Provisioning] Onboarding triggered for tenant ${tenantId}, user ${userId}`);
}
